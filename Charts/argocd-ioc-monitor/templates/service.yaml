apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-ioc-monitor.fullname" . }}
  labels:
    {{- include "argocd-ioc-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-ioc-monitor.selectorLabels" . | nindent 4 }}
